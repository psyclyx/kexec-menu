[workspace]
members = ["crates/kexec-menu", "crates/kexec-menu-core"]
resolver = "2"

[workspace.package]
version = "0.6.0"
edition = "2021"
license = "MIT"

[profile.release]
opt-level = "z"
lto = true
codegen-units = 1
strip = true
panic = "abort"
